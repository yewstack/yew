[package]
name = "build-examples"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
reqwest = { version = "0.12.12", features = ["blocking", "json"] }
regex = "1.11.3"
toml = "0.9.7"
serde = { version = "1.0.227", features = ["derive"] }
