# Server-side Rendering with warp & reqwest

This example demonstrates server-side rendering with warp & reqwest.

Run `cargo run -p yew-ssr-warp` and navigate to http://localhost:8080/ to
view results.
